<!DOCTYPE html>
<html>

<head>
    <title>User Details</title>
    <link rel="stylesheet" href="../styles/style.css">
</head>

<body>
    <% include global/header %>

        <div id="enter_name">
            <h2>
                <%if (user==null){%> Create Account
                    <%} else{%>User Details:
                        <%=user.name%>
                            <%}%>
            </h2>
            <form action="<%if(user==null){%>/users<%}else{%>/user/<%=user.name%>?_method=PUT<%}%>" method="post">
                Username: <input type="text" id="enter_username" name="name" value="<%if (user!=null){%><%=user.name%><%} else {%><%}%>"><br style="line-height: 40px;"> Password: <input type="text" id="enter_password" name="password" value="<%if (user!=null){%><%=user.password%><%} else {%><%}%>"><br style="line-height: 40px;"> Zip code: <input name="zipcode" list="zipcode">
                <datalist id="zipcode">
                <option value="Internet Explorer">
                <option value="Firefox">
                <option value="Chrome">
                <option value="Opera">
                <option value="Safari">
                </datalist> <br style="line-height: 20px;"><br style="line-height: 20px;"> Neighborhood:
                <input name="neighborhood" list="neighborhood">
                <datalist id="neighborhood">
                <option value="Internet Explorer">
                <option value="Firefox">
                <option value="Chrome">
                <option value="Opera">
                <option value="Safari">
                </datalist><br style="line-height: 20px;">
                <br style="line-height: 20px;">
                <button type="submit" id="enter_name_button"><%if(user==null){%>Create Account<%}else{%>Modify Account<%}%></button>
            </form>
            <br>
            <% if (user!=null) {%>
                <form action="/user/<%=user.name%>?_method=DELETE" method="post">
                    <input type="submit" value="Delete User">
                </form>
                <%}%>
                    <% if (user!=null) {%>
                        <form action="/playAgain" method="get">
                            <button type="submit" id="play_again">Play</button>
                        </form>
                        <%}%>
        </div>
        <footer>
            <% include global/footer %>
        </footer>
        <script src="../scripts/scripts.js"></script>
</body>

</html>
